"""
Reference models assembled from the layers in :mod:`rotorch.nn`, grouped by architecture.

Each model reproduces one of the experiments of the paper its architecture comes from, so they
double as worked examples of how the layers fit together. So far those are
:mod:`rotorch.models.cgenn` and :mod:`rotorch.models.gatr`; others are to follow.
"""
